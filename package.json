{
  "name": "youtube-home-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.e2e.json && pnpm -r --if-present typecheck",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "test": "pnpm test:unit",
    "test:unit": "vitest run",
    "test:db:integration": "vitest run --config vitest.integration.config.ts --configLoader runner",
    "test:auth:integration": "pwsh -NoProfile -File scripts/test-phase1-db.ps1",
    "test:phase2:collectors": "vitest run packages/collectors apps/worker/src/jobs packages/shared/src/channel-*",
    "test:e2e": "playwright test",
    "test:integration": "pwsh -NoProfile -File scripts/test-phase1-docker.ps1",
    "db:generate": "prisma generate",
    "db:validate": "prisma validate",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "docker:up": "docker compose up -d --build --wait",
    "docker:pull": "docker compose -f docker-compose.prebuilt.yml pull",
    "docker:play": "docker compose -f docker-compose.prebuilt.yml up -d --wait",
    "docker:down": "docker compose down",
    "docker:health": "pwsh -NoProfile -File scripts/health-check.ps1",
    "verify": "pnpm db:validate && pnpm db:generate && pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build",
    "verify:phase1": "pnpm verify && pnpm test:auth:integration && pnpm test:integration",
    "verify:phase2": "pnpm verify && pnpm test:phase2:collectors && pnpm test:auth:integration && pnpm test:integration"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "@vitejs/plugin-react": "6.1.0",
    "@vitest/coverage-v8": "4.1.11",
    "@yt-monitor/shared": "workspace:*",
    "dotenv": "17.4.2",
    "eslint": "10.9.0",
    "globals": "17.11.0",
    "prettier": "3.9.6",
    "prisma": "7.9.1",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0",
    "vitest": "4.1.11"
  }
}
