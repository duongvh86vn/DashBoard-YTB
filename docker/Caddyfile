http://{$CADDY_SITE_ADDRESS} {
  encode gzip

  header {
    -Server
    X-Content-Type-Options "nosniff"
    Referrer-Policy "strict-origin-when-cross-origin"
    X-Frame-Options "DENY"
  }

  handle /api/* {
    reverse_proxy api:5000
  }

  handle {
    reverse_proxy web:3000
  }
}
